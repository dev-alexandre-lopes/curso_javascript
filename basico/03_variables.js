//JavaScript Variables:

/*
Todas as variáveis ​​JavaScript devem ser identificadas com nomes exclusivos.

Esses nomes exclusivos são chamados de identificadores.

Os identificadores podem ser nomes curtos (como x e y) ou nomes mais descritivos (idade, soma, volume total).

As regras gerais para construir nomes para variáveis ​​(identificadores únicos) são:

1) Os nomes podem conter letras, dígitos, sublinhados e cifrões;

2) Os nomes devem começar com uma letra;

3) Os nomes também podem começar com $ e _ ;

4) Os nomes diferenciam maiúsculas de minúsculas (y e Y são variáveis ​​diferentes) Case Sensitive;

5) Palavras reservadas (como palavras-chave JavaScript) não podem ser usadas como nomes;

Em javacript podemos usar CamelCase para definir o nome de variáveis e constantes. 

Por exemplo: 

let = nomeCliente;

const = nomeCompleto;

*/

let NomeCliente = 'Paulo'; // Javascript é case sensitive

let nomeCliente = 'João'; // Iniciando uma variável

let $$$ = "Hello World";

let $ = 2;

let $myMoney = 5;

let _ultimoNome = "Souza";

let _x = 2;

let _100 = 5;

console.log(nomeCliente);

console.log($, $$$, $myMoney);

console.log(NomeCliente, _ultimoNome);